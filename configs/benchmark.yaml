backends:
  - name: mlx-lm
    models:
      - mlx-community/Llama-3.2-3B-Instruct-4bit

  - name: ollama
    models:
      - llama3.2:3b

prompts:
  - builtin: short_qa
  - builtin: app_generation

settings:
  max_tokens: 2048
  runs_per_config: 3
  warmup_runs: 1
  cool_down_seconds: 2.0
